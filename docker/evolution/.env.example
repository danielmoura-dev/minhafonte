# URL pública da Evolution API (na VPS use o IP/domínio real)
SERVER_URL=http://localhost:8080

# Chave de autenticação da API — gere uma forte (ex: openssl rand -hex 32)
# e use a MESMA no .env do Laravel como EVOLUTION_API_KEY
AUTHENTICATION_API_KEY=troque-esta-chave
